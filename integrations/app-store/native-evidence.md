# Apple App Store — Native Evidence

## Verified

```text
bundle identifier: com.aveli.aveli
deployment target: iOS 15.0
RevenueCat mobile integration: purchases_flutter
subscription-management URL: https://apps.apple.com/account/subscriptions
```

`Info.plist` includes usage descriptions for camera, photo library and contacts.

## External Configuration Evidence

Production product ids, subscription group, provider offering layout, StoreKit/provider configuration and real-store E2E results are not verified repository facts. Test-fixture names such as `aveli_support_monthly` / `aveli_support_yearly` are not treated as production ids.

No relevant `.entitlements` file was established by the inspected evidence. These are external release-configuration evidence items, not architecture blockers.

Pricing shown by Aveli comes from provider/store localized product data through RevenueCat.
